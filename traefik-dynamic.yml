http:
  routers:
    proxy:
      rule: "Host(`dreamhouse237.duckdns.org`)"
      entryPoints:
        - websecure
      tls:
        certResolver: myresolver
      service: proxy-service

  services:
    proxy-service:
      loadBalancer:
        servers:
          - url: "http://proxy-service:8080"