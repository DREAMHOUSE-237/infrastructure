http:
  routers:
    proxy:
      rule: "Host(`api.16.171.110.76.nip.io`)"
      entryPoints:
        - websecure
      tls:
        certResolver: myresolver
      service: proxy-service

  services:
    proxy-service:
      loadBalancer:
        servers:
          - url: "http://proxy-service:8080"